
export interface InfoPagina {
//usamo el json to ts para pasar la info, lo que se hace es copiar el json y activar el json to ts y listo
  titulo?: string;
  email?: string;
  username?: string;
  pagina_autor?: string;
  facebook?: string;
  instagram?: string;
  equipo_trabajo?: any[];
}
